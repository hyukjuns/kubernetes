apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: httpd-external
  namespace: myweb
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx-external
  rules:
  - host: external.testcloud.site
    http:
      paths:
      - backend:
          service:
            name: httpd-external
            port:
              number: 80
        path: /
        pathType: Prefix
---
apiVersion: v1
kind: Service
metadata:
  name: httpd-external
  namespace: myweb
spec:
  type: ClusterIP
  ports:
    - port: 80
  selector:
    app: httpd